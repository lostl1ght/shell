gui:
  enlargedSideViewLocation: top
  theme:
    lightTheme: false
    selectedLineBgColor:
      - black
  showRandomTip: true
  showCommandLog: true
  nerdFontsVersion: 3
git:
  paging:
    colorArg: always
    # pager: 'delta --dark --paging=never'
    # pager: 'delta --dark --paging=never --diff-so-fancy'
    pager: 'delta --dark --paging=never --diff-highlight'
confirmOnQuit: false
quitOnTopLevelReturn: false
os:
  editPreset: 'nvim'
disableStartupPopups: true
notARepository: 'quit'
promptToReturnFromSubprocess: false
